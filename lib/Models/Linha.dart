import 'package:flutter/cupertino.dart';
import 'Pedido.dart';

class Linha{
  TableRow linha = TableRow(children: List.from(pedido(1, 123, 1234, 1, 123, 'teste', 'Pedido', 'teste') as Iterable)
  );
}